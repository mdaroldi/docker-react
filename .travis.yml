language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t mdaroldi/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true mdaroldi/docker-react npm run test